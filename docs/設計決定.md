# 設計決定 (Architecture Decision Records)

Compassの設計に関する重要な意思決定を記録します。

## テンプレート

新しいDecisionを追加する際は、以下のフォーマットを一番上（Decisionsのすぐ下）に追加してください。

### D-[番号]: [タイトル]
- **日付**: YYYY-MM-DD
- **ステータス**: Proposed / Accepted / Rejected / Deprecated
- **背景**: なぜこの決定が必要になったのか
- **決定内容**: 何を決定したのか
- **理由**: なぜそのように決定したのか（他の選択肢との比較など）
- **影響範囲**: この決定によって影響を受けるコンポーネントやフロー

---

## Decisions

### D-0006: 睡眠情報をDailyLogから分離しSleepRecordとして管理する
- **日付**: 2026-07-21
- **ステータス**: Accepted
- **背景**: DailyLogは1日に複数回記録できる一方、睡眠は起床日単位で原則1件として扱う方が分析・編集・将来のスマートウォッチ連携に適している。DailyLogに `sleepHours` を持たせ続けると、同日に複数ログを作成した場合に睡眠の再入力や重複が発生する。
- **決定内容**: 睡眠情報はDailyLogから分離し、`SleepRecord` として `sleepDate` 単位で管理する。睡眠時間は就寝日時と起床日時から自動計算し、UIではRepositoryを直接操作せずApplication Serviceを経由する。永続化はMVP方針に合わせてlocalStorageを使う。旧DailyLogの `sleepHours` は既存データを破壊しないため互換読み取り用に一時的に残し、新規入力ではSleepRecordを正とする。
- **理由**: DailyLogの複数記録性と睡眠の1日1件性を分離し、入力ミス修正、日付またぎ睡眠、分単位計算、将来のスマートウォッチ由来データを安全に扱うため。
- **影響範囲**: `src/features/sleep/`、DailyLog入力UI、DailyLog型の `sleepHours` 互換方針、将来のSleepAnalyzerとAnalysis入力データ。

### D-0001: ドキュメントの日本語化とDecision管理の導入
- **日付**: 2026-07-15
- **ステータス**: Accepted
- **背景**: Compassプロジェクトの設計・議論は日本語で行われており、「国際化」よりも「開発効率」と「視認性」を優先するため、人間とAIが迷わず理解できるリポジトリを目指す必要があった。
- **決定内容**: `docs/` 配下の主要ドキュメント（ビジョン、設計原則、要件定義、変更履歴、リポジトリガイドなど）のファイル名を日本語化する。設計上の重要な意思決定を記録するため、ADR形式のDecision管理（本ファイル）を導入する。開発フローを「アイデア → 議論 → レビュー → Decision（正式採用） → ドキュメント更新 → 実装 → 変更履歴」へ変更する。
- **理由**: ドキュメントを「人にもAIにも分かりやすい資産」として育て、Founderが迷わず開発を進められるようにするため。将来的なOSS化・英語化は設計が固まった後に検討する。
- **影響範囲**: `docs/` 配下のファイル名、AI Collaboration Protocol、AI Context、リポジトリガイド、変更履歴などの各ドキュメント。

### D-0002: User Model（人物理解）の二層構造と仮説検証システムの設計
- **日付**: 2026-07-15
- **ステータス**: Accepted
- **背景**: Compassのコア思想（Core Philosophy）に基づき、AIは単なる会話ログの保存ではなく「ユーザー理解の継続的なアップデート」を行う必要があります。しかし、人間の特性（長期的な価値観と、短期的な感情や状況の変化）を一つのデータ構造に混在させると、AIの空気の読めない推論（Reasoning）が発生しやすくなります。また、AIが過去の発言からユーザーの性格を決めつける「ハルシネーションの固定化」を防ぐため、すべての理解は「事実（Evidence）に基づく仮説（Hypothesis）」として管理される必要がありました。
- **決定内容**: 
  User Modelを以下の3つの論理レイヤーに分離して設計・実装します。
  
  1. **長期・静的レイヤー（Long-term Layer: "家の柱"）**：価値観（coreValues）、夢や長期目標（longTermGoals）、性格傾向（personalityTraits）などの本質的情報。
  2. **短期・動的レイヤー（Short-term Layer: "吹き抜ける風"）**：現在の気分（currentMood）、直近の悩み（immediateConcerns）、最近の興味（recentInterests）などのリアルタイムな状態。
  3. **仮説・根拠レイヤー（Hypothesis & Evidence Layer: "推理ノート"）**：各データを単なるテキスト値ではなく、確信度（confidence）と、その根拠となった会話ログの参照（evidenceList）を持つ `Hypothesis` オブジェクトとして管理する。

  具体的なデータ構造（TypeScript Schema）は以下の通り定義します。

  ```typescript
  interface UserModel {
    id: string;
    longTerm: {
      coreValues: Hypothesis<string[]>;
      longTermGoals: Hypothesis<string[]>;
      personalityTraits: Hypothesis<string[]>;
    };
    shortTerm: {
      currentMood: {
        status: string;
        intensity: number; // 1-5 (例: 1=軽度、5=極めて重度)
        lastUpdated: string;
      };
      immediateConcerns: Hypothesis<string[]>;
      recentInterests: Hypothesis<string[]>;
    };
  }

  interface Hypothesis<T> {
    value: T;
    confidence: number; // 0.0 - 1.0 (確信度)
    evidenceList: Array<{
      logId: string;
      extractedText: string;
      timestamp: string;
    }>;
    lastUpdated: string;
  }
  ```
- **理由**: 長期的な本質と短期的な状態を分け、さらに全てを根拠付き仮説として扱うことで、Compassがユーザーを固定的にラベリングせず、変化し続ける存在として理解できるようにするため。
- **影響範囲**: `src/features/compass-map/types/userModel.ts`、分析・Reflection・Memory生成フロー、Compass Map UI。

### D-0003: User Modelの提示・検証体験（Compass MapとReflection）の設計
- **日付**: 2026-07-16
- **ステータス**: Accepted
- **背景**: D-0002で設計したUser Modelの情報を、どのようにユーザーに提示するべきかという「体験（UX）」の設計が必要であった。AIが理解した内容（性格、悩み、価値観など）を完全にブラックボックス（裏側のみ）にすると「根拠を説明する」という原則に反し不信感を生む。一方で、画面に常時表示すると「AIによるラベリング（決めつけ）」の圧迫感を与え、「鏡に毎日『あなたはこういう人です』と表示されるような違和感」を生んでしまう。
- **決定内容**:
  User Modelの提示と検証のプロセスを、以下の「3層構造」として設計・実装します。

  1. **① Invisible Understanding（常時裏側 / 潜伏する理解）**
     * 役割：AIの推論（Reasoning）の文脈として常にバックグラウンドで機能する。
     * 挙動：会話の裏糸として、提案のトーン＆マナーや、さりげないサポート内容のパーソナライズに活用する。
  2. **② Reflection（時々見せる / 共同検証の儀式）**
     * 役割：定期的に（例：月1回や、特定の対話の節目など）、AIの理解をユーザーに開示し、答え合わせをする。
     * UI表現：チャット内にカード形式で提示。「最近、あなたについて〇〇という傾向が見えてきたのですが、合っていますか？」と問いかけ、ユーザーが「そう思う（確信度UP）」「違う（確信度DOWN/修正）」をフィードバックできる学習ループを作る。
  3. **③ Compass Map（見たい人だけ / 航海図としてのプロフィール）**
     * 役割：自分の「取扱説明書」であり、現在地と未来への旅路を記録した「自己理解の地図」。
     * UI表現：プロフィール画面や専用のダッシュボード。固定された人格診断ではなく、AIとユーザーが一緒に描いていく「航海図（Compass Map）」としてビジュアル化し、いつでもユーザー自身が直接修正・削除できるようにする。

- **理由**: User Modelを「AIが勝手に作るユーザー像」ではなく、「AIとユーザーが一緒に育てる自己理解の地図」として位置づけるため。これにより、AIの押し付けがましさを排除し、透明性とコントロール権をユーザーに返しながら、強固な信頼関係（ラポール）を築くことができるため。
- **影響範囲**: フロントエンドの画面コンポーネント（Compass Map画面、Reflection表示用カード）、および会話・推論エンジン（チャットUIとの統合ロジック）。
### D-0004: Compass Analysis Persona（推論エンジンの基本思想と5原則）

## 概要
Compassの裏側で動く推論エンジン（LLM）が、ユーザーのログを分析する際の「人格（ペルソナ）」と「基本姿勢」を定義する。AIは「評価する人」ではなく、「静かな観察者であり、理解者」として振る舞う。

## 決定内容
ログ分析において、以下の5つの基本原則（Compass Analysis Persona）をシステムプロンプトの核として厳格に適用する。

* **① 判断より理解を優先する:** ユーザーの行動や状態に対して「良い・悪い」のジャッジを下さない。「なぜそうなったのか」という背景と、その奥にある価値観の探索に集中する。
* **② ネガティブを「問題」と決めつけない:** 「疲労」や「やる気が出ない」といったネガティブな状態を直ちに改善すべき欠点として扱わない。それらを「自分を守るためのサイン（回復要求など）」として肯定的に解釈する。
* **③ 行動ではなく「パターン」を見る:** 1日の単発的な出来事（点）でユーザーを断定しない。過去の記録との連なり（線）から、環境・体調・感情の「変化のパターン」を読み取る。
* **④ 強みを発見する（資源の探索）:** 課題や問題点の指摘ではなく、その人の中にある「強み」や「大切にしていること（リソース）」を見つけ出すことにAIの推論能力を注ぐ。
* **⑤ 仮説として扱う:** 抽出した価値観や性格傾向を「あなたは〇〇な人です」と絶対に断定（ラベリング）しない。常に「〜かもしれません」「〜という傾向があるようです」という仮説として提示し、決定権をユーザーに委ねる。

## 理由
Compassを「便利な自己管理アプリ」ではなく、「本当に自分を理解してくれる伴走者（航海士）」として機能させるため。ユーザーに「評価・監視されている」という不気味さを与えず、「無条件の肯定的関心」に基づいた心理的安全性の高い体験を提供するためには、AIの分析姿勢をシステムレベルで明確に定義・制限する必要がある。

### D-0005: Compassの理解処理は二段階モデルを採用する

## 概要
ユーザーの入力を受け止める「Immediate Response（即時反応）」と、過去のデータから文脈を読み解く「Reflection（深い理解）」の処理タイミングと役割を明確に分離する。

## 決定内容
Compassは、リアルタイム応答と非同期のバッチ処理（Reflection）を組み合わせたハイブリッドアーキテクチャを採用する。

1. **Immediate Response（即時反応）**
   * **タイミング:** ユーザーがDaily Logを保存した直後。
   * **役割:** 受信と安心感の提供。
   * **処理内容:** ログが安全に保存されたことの通知と、「今日も一日お疲れさまでした」といった、その日の感情や出来事に対する浅く温かい傾聴と受容のみを行う。ここでの深い人格分析やUserModelの更新は**絶対に**行わない。

2. **Reflection（深い理解）**
   * **タイミング:** 非同期（夜間バッチ処理、または一定期間経過後）。
   * **役割:** パターンの発見と深い人格理解。
   * **処理内容:** 過去のログ、環境データ、睡眠、行動パターンなどの蓄積データをLLMが総合的に分析する。「最近のあなたについて分かったこと」として仮説を生成し、適切なタイミングでUserModel更新の確認（提案）をユーザーに行う。

## 理由
1. **人間の自然な理解プロセスの模倣:** 人間同士の関係構築と同様に、一人の人間を深く理解するには1日の出来事（点）だけでなく、時間の蓄積（線）を見る必要があるため。
2. **誤ったラベリングの防止:** 1日の断片的な感情からAIが短絡的な人格判断を下すことを防ぎ、「評価者」ではなく「静かな観察者」としてのペルソナを担保するため。
3. **「航海士」という世界観との合致:** 航海士が一つの波を見るだけでなく、風、潮流、過去の航路を総合して進路を提案するように、Compassも全体の文脈からユーザーの価値観を抽出するため。
