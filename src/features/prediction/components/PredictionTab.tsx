import { useState } from 'react';
import { predictionQueryService } from '../services/compositionRoot.ts';
import type { PredictionReadModel } from '../types/prediction.ts';
import { formatPredictionNumber } from './predictionPresentation.ts';
import './PredictionTab.css';
export function PredictionTab() { const [model,setModel]=useState<PredictionReadModel>(()=>predictionQueryService.getTomorrowOutlook()); return <section className="prediction"><header><p className="prediction-eyebrow">保存済みデータから見る</p><h2 id="prediction-primary-heading" tabIndex={-1}>明日の見通し</h2><p>明日の天気と、これまでの雨の日・雨でない日の疲労記録だけを条件付きで見比べます。</p><button type="button" onClick={()=>setModel(predictionQueryService.getTomorrowOutlook())}>記録を読み直す</button></header><article className="prediction-card"><p className="prediction-status">{statusLabel(model.status)}</p><h3>{model.headline}</h3><p>{model.explanation}</p>{model.status==='OUTLOOK_AVAILABLE'&&<dl><div><dt>対象日</dt><dd>{model.targetLocalDate}</dd></div><div><dt>予報条件</dt><dd>{model.forecastCondition==='RAIN_EXPECTED'?'雨が予想されます':'雨は予想されていません'}</dd></div><div><dt>比較グループ</dt><dd>{model.comparisonGroupLabel}</dd></div><div><dt>過去の平均疲労</dt><dd>{formatPredictionNumber(model.comparisonAverageFatigue)}</dd></div><div><dt>別グループとの差</dt><dd>{formatPredictionNumber(model.historicalDifference)}</dd></div><div><dt>見通しの信頼度</dt><dd>{model.predictionConfidence==='HIGH'?'比較的高い':'中くらい'}</dd></div></dl>}<p className="prediction-caution">{model.caution}</p><details><summary>使った記録を確認</summary><p>Forecast Snapshot: {model.forecastSnapshotId??'なし'}</p><p>日々の記録: {ids(model.relationshipDailyLogIds)}</p><p>過去の天気記録: {ids(model.relationshipHistoricalWeatherRecordIds)}</p></details></article></section>; }
function ids(v:readonly string[]){return v.length?v.join('、'):'なし';}
function statusLabel(s:PredictionReadModel['status']){return ({SETTING_REQUIRED:'設定が必要です',FORECAST_UNAVAILABLE:'予報を利用できません',RELATIONSHIP_UNAVAILABLE:'比較を利用できません',INSUFFICIENT_CONFIDENCE:'信頼できる記録が不足しています',OUTLOOK_AVAILABLE:'条件付きの見通し'})[s];}
